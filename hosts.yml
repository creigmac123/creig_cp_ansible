all:
  #vars:
  #  ansible_connection: ssh
  #  ansible_user: mysmith
#    ansible_become: true
#    ansible_password: Summer@022020
    #ansible_ssh_private_key_file: /tmp/certs/ssh_priv.pem

zookeeper:
  hosts:
    mysmith-adm01:

kafka_broker:
  hosts:
    mysmith-adm01:


schema_registry:
  hosts:
    mysmith-adm01:

kafka_rest:
  hosts:
    mysmith-adm01:

ksql:
  hosts:
    mysmith-adm01:

kafka_connect:
  hosts:
    mysmith-adm01:

control_center:

  hosts:
    mysmith-adm01:
